# 往来账龄明细的最终语义与数据字典一致性审查

当前 SQL 以统驭凭证行覆盖加非统驭余额追加生成月末往来账龄，数据字典同步描述规则、粒度和运行约束。静态审查未发现确定性代码缺陷：账龄优先级和公式符合指定口径，配置连接不会复制事实行，24 个事实源与 6 个余额源没有遗漏或字段错位，DDL、两段 INSERT 和两段最终投影均保持 58 列。生产放行仍取决于已明确记录的外部数据与调度前提，其中 S610 余额物理来源是最高优先级确认项。

Watch for: **likely（高，上线前需确认）** — `ods_s680_faglflext` 若不承载 `RCLNT=610`，S610 非统驭余额会被配置连接静默过滤；**confirmed dependency（高，上线前需确认）** — 账龄配置要求有效期可与 `YYYYMM` 正确比较、空公司确为系统级语义且配置值精确为 `基准日期-zfdat`；**confirmed dependency（高，上线前需确认）** — 源端日期比较和解析依赖统一 `YYYYMMDD` 契约，余额累计依赖自然年财务变式。

**Verdict**: NEEDS_CHANGES

## High-level view

第一段严格按 AB 凭证、公司级配置、系统级配置、默认凭证日期的顺序选择账龄起算日，再以参数月末减起算日加一计算账龄。配置先按修剪后的系统和空串归一化后的公司分组；公司级和系统级同时存在时，两次连接仍只产生一条事实结果。

六个系统的 BSID、BSAD、BSIK、BSAK 共 24 个分支都在物理源处应用月末过滤。未清源统一要求 `BUDAT <= 月末` 并排除 BSTAT A/S，已清源额外要求 `AUGDT > 月末`；把相同谓词从 `all_items` 下推到每个分支保持原快照集合不变。

24 个事实分支与 6 个余额分支分别保持同一字段契约。DDL、两段 INSERT 列清单和两段最终 SELECT 都是从 `dt_month` 到 `load_dt` 的 58 列同序结构；直接 NULL 有 INSERT 目标类型，UNION 内的 NULL 位置也有具体类型分支参与推断。

数据字典准确反映优先级、过滤下推、非幂等追加、直接 NULL 字段和 S610 暂用来源。剩余事项是文档已记录的环境或调度前提，不属于已确认的 SQL 缺陷，但完成验证前不能批准生产上线。

<details>
<summary>Issues (5)</summary>

1. **高｜S610 余额来源（likely，上线前需确认）** — `ods_s680_faglflext` 若不实际承载 `RCLNT=610`，S610 余额会静默消失；核验物理表及客户端值，必要时改用真实 S610 来源或固定正确系统标识。
2. **高｜账龄配置契约（confirmed dependency，上线前需确认）** — SQL 依赖 `valid_fr/valid_to` 可按 `YYYYMM` 比较、空公司代表系统级且 `agingdate` 精确命中；核验字段类型、空值语义和实际配置值，若 NULL `valid_to` 代表长期有效则需先统一契约或显式处理。
3. **高｜SAP 日期值域（confirmed dependency，上线前需确认）** — BUDAT/AUGDT 在解析前与八位字符串比较，日期投影又依赖目标 Doris 对 `00000000` 的解析行为；核验六系统类型和值域，并在目标版本确认非法日期返回 NULL 而非报错。
4. **中｜财年期间映射（confirmed dependency，上线前需确认）** — 余额段直接把自然年/月用作 RYEAR 和 HSL/TSL 期间；确认所有相关公司采用自然年财务变式，否则按财年变式换算。
5. **中｜余额追加重试（confirmed operational constraint）** — 第二段普通 INSERT 单独重跑会重复追加余额；调度必须将先覆盖、后追加绑定为不可拆分的月份重跑入口。

</details>

<details><summary>Details</summary>

## AB、公司级与系统级规则的账龄起算顺序

`items_with_aging_date` 在 `dwd_fi_mr_arap_detail_mi.sql:716-720` 依次判断 `BLART='AB'`、公司级配置、系统级配置，最后返回 `BLDAT`。AB 直接返回 `ZFBDT`，没有空值回退；无法解析时该行会被后续非空日期条件排除，而不会改用 BLDAT。账龄在第 763-767 行按 `DATEDIFF(参数月末, aging_date_sap) + 1` 计算，写入的 `baseline_dt` 使用同一个 `aging_date_sap`。

`agingdate_cfg` 在第 703-712 行按有效期和 `TRIM(agingdate)='基准日期-zfdat'` 过滤，再按 `TRIM(system_src)` 与 `NULLIF(TRIM(company_code),'')` 分组。第 722-728 行分别连接公司键和空公司系统键，每侧对任一键至多一行；两类配置同时存在时连接基数仍为 1×1，CASE 优先识别公司级配置。

**confirmed dependency（高，上线前需确认）:** SQL 直接用 `r.valid_fr <= p.dt_month` 和 `r.valid_to >= p.dt_month` 比较六位字符串，NULL `valid_to` 不会命中。目标维表必须确认字段类型/格式、空公司语义和配置实际值；若 NULL 代表长期有效，需要先统一数据契约或显式处理。

## 24 个事实源的月末快照下推

静态计数确认四类表各有 S600、S700、S800、S900、S610、S810 六个分支。24 个分支都有 `BUDAT <= key_date_sap` 和排除 BSTAT A/S 的条件，BSAD/BSAK 的 12 个已清分支还各有 `AUGDT > key_date_sap`；没有分支遗漏或条件不一致。

参数 CTE 只有一行，同一谓词由 UNION 后移到每个输入分支不改变记录集合；后续 `account_cfg` 仍按系统、公司、科目及 D/K 类型筛选。因此删除 `all_items` 中重复的 `CROSS JOIN params` 和 WHERE 保持原快照语义。

**confirmed dependency（高，上线前需确认）:** 源端谓词在解析前把 BUDAT/AUGDT 与 `YYYYMMDD` 字符串比较。六系统字段必须具有可稳定比较的统一类型和值域。

## UNION 字段契约与 58 列写入边界

24 个事实分支均为 24 个同序表达式；客户源的 `RSTGR` 与供应商源同位置的 NULL 在 `all_items` UNION 中归并。6 个余额分支也保持同序，S800/S900 的利润中心 NULL 位于前序 FAGLFLEXT 分支已提供具体类型的位置。

自动检查确认 DDL、第一段 INSERT 清单及最终 SELECT、第二段 INSERT 清单及最终 SELECT 均为 58 列；两份 INSERT 清单与 DDL 字段名逐位一致，最终投影都从 `dt_month/year/month` 开始，以 `system_src/ods_src/load_dt` 结束。直接 NULL 的渠道、商冷客户群、线上线下、映射后利润中心、业务范围名称、业务管理单元和账龄段均有明确 INSERT 目标类型，未发现无类型 NULL 或列移位风险。

## 日期初始值与数据字典同步

目标 SQL 已移除 `00000000` 的显式二次 `NULLIF`，统一调用 `STR_TO_DATE(NULLIF(TRIM(value),''),'%Y%m%d')`。起算日期转换失败时事实行被排除，清账日期转换失败时保留 NULL；目标 Doris 版本仍需最小验证 `STR_TO_DATE('00000000','%Y%m%d')` 返回 NULL 而不是报错。

字典 `数据字典与ER关系知识库.md:17995-18004` 与 SQL 一致地说明了起算优先级、配置去重、源端过滤、余额来源、直接 NULL 范围及先覆盖后追加；字段表第 18018、18058 行也与 `baseline_dt` 和 `aging_days` 实现一致。文档没有继续声称存在已移除的 `00000000` 显式判断。

## S610 来源与余额运行前提

**likely（高，上线前需确认）:** `dwd_fi_mr_arap_detail_mi.sql:1194` 读取 `ods.ods_s680_faglflext`，并由 `RCLNT` 动态生成 `system_src`。若该表返回 680 而非 610，S680 无法匹配六套目标系统配置，S610 余额会被静默丢弃。脚本第 1381-1382 行和字典第 18004 行已准确把它列为外部确认项，并未将其伪装成已解决的来源。

**confirmed dependency（中，上线前需确认）:** 余额段直接把参数自然年/月用于 RYEAR 和 HSL/TSL 累计，只适用于自然年财务变式；部署前需按公司核验。

**confirmed operational constraint（中）:** 第二段普通 INSERT 单独重试会重复余额。脚本和字典均要求完整重跑先覆盖再追加，调度必须把两段绑定为一个入口。

</details>

<details>
<summary>File map</summary>

- `01_代码/01_DWD/C_应收应付/dwd_fi_mr_arap_detail_mi.sql` — 六系统统驭行项目快照与非统驭余额的 58 列月末装载。
- `99_AI/数据字典与ER关系知识库.md` — 往来账龄表的血缘、规则、字段口径和上线前约束。

Full diff: 当前仓库的 `main` 尚无提交，目标文件均未跟踪，无法生成相对基线 diff；本次按两份当前文件及现有账龄参考脚本进行只读静态审查。

</details>
