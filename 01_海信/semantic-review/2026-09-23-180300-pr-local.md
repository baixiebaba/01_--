# ARPM02 汇率换算与聚合前处理

本次修改在 `CURRENT_SOURCE` 中按来源行计算汇率换算，将交易币 QCY 金额转换为本位币 BCY 后再进入来源聚合。`COD_AZIENDA='2023'` 使用 SAP `TCURR`，其他公司使用以人民币为基准的 `DATI_CAMBIO`，并对缺失或零汇率回退到原 BCY 金额。BCY_0 至 BCY_15 均使用同一分流后的转换因子，计算位置也在 `CURRENT_AGG` 的 `SUM` 之前。Watch for: **confirmed** 本次限定检查范围内未发现阻断问题。

**Verdict**: APPROVED

## High-level view

换算在 `CURRENT_SOURCE` 的逐来源层完成，随后才按来源类别汇总；2023 走 TCURR，其他公司走 DATI_CAMBIO，缺失或零汇率保留原 BCY。新增 CTE 和 `CROSS APPLY` 嵌入现有 `MERGE ... USING (WITH ...)` 结构，未见明显 Oracle 结构问题。

<details>
<summary>Issues (0)</summary>

未发现本次限定范围内的可执行问题。

</details>

<details>
<summary>Details</summary>

### `CURRENT_SOURCE` 的 0—15 桶换算

**confirmed**：第 215—310 行对 BCY_0 至 BCY_15 统一使用 `FX.CONVERSION_FACTOR`；仅当该因子存在时才以对应 QCY 桶相乘，否则保留对应原 BCY 桶。第 337—369 行把空白汇率评估标识、币种非空和汇率有效性集中到同一因子判断中。

### 2023/非2023汇率分流与回退

**confirmed**：2023 分支只使用 `TCURR_CONV.RATE`，非2023分支只使用 `QCY_RATE`、`BCY_RATE` 的 DATI_CAMBIO 比值；两类分支均排除零值。非2023任一端缺失或为零时条件不成立，因子为 NULL，随后回退原 BCY，而不是将金额置零。

### 聚合顺序与 Oracle 结构

**confirmed**：换算后的金额在 `CURRENT_SOURCE` 中形成，`CURRENT_AGG` 第 436—485 行才执行各桶和来源类别的 `SUM`，满足聚合前计算要求。静态括号检查通过，`git diff --check` 无输出；SQLFluff 因文件超过默认解析大小限制而跳过，未进行数据库运行时编译。

</details>

<details>
<summary>File map</summary>

- `01_代码/02_DWM/C_应收应付/CPM_SP_M2M_ARP_AG_M_PHASE2.sql` — 汇率分流、缺失/零值回退及聚合前 BCY 桶换算。
- 完整变更：`git diff main -- 01_代码/02_DWM/C_应收应付/CPM_SP_M2M_ARP_AG_M_PHASE2.sql`

</details>
