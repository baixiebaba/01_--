# 2026-09-24

## 阅读数据字典与ER关系知识库
- 文件路径：`/Users/zhouhong/Documents/01_TA工作/01_海信/99_AI/数据字典与ER关系知识库.md`（2.5MB / 64488 行 / 2381张表）
- 已梳理六大结构块，并定位应收应付相关条目行号（DWD_FI_AR_AGE_DETAIL_MI@17492、SMS_BALANCE@18090、ARAP_SUM@64391、GP_MSUM_CKTH@5342、FORM_DATI@5488）
- 编写账龄三方校验 SQL（`校验_管报2.0_ARPM02_vs_管报1.0_账龄.sql`）：AW_MR9_ARPM02_000001(TGK_FIMA_DEV) vs FORM_DATI(TGK_GB_HISENSE, ZS_AR/AP0001_IPT01) vs DATI_SALDI_LORDI。两查询：①客商级 2.0vs1.0 差异；②公司+项目级三方互核（DATI_SALDI_LORDI 无客商维度，年初/上月/本月取去年12月/上月/本月三个 IMPORTO 快照，科目前缀匹配，合同资产=146200A01+1910000A30）。待确认：两系统场景值、COD_PERIODO 格式、dati_saldi_lordi 的 COD_CATEGORIA 过滤、金额正负号口径
- 单体法人表科目限制改为走科目层级视图：`TGK_GB_HISENSE.V_REF_CONTO`（HIE/NODE/ELEM/DESC0/DESC_ELEGER0），`WHERE hie='01' AND node=节点编码` 取 ELEM，再限制 `DATI_SALDI_LORDI.COD_CONTO`（弃用前缀 LIKE）。新增查询3前置检查：节点→要素展开行数、同 ELEM 挂多节点重复风险、是否需 CONNECT BY 递归
- 校验 SQL 按用户要求合并为单查询：参数化（p_scn_10/p_scn_20/p_month/p_azienda，公司 NULL=全部），往来项目粒度固定 10 行输出（以 itm_dedup 为主表 LEFT JOIN 三源），12 列金额（三源×年初/上月/本月 9 列 + 差异 3 列=2.0−1.0），行序按用户指定；中文列别名加双引号
- 校验 SQL 新增查询2（客商级）：粒度 公司+往来项目+客商，新增 公司/客商编码/客商名称 三列，不含单体法人；2.0 客商 `CUST_CODE`+`CUST_NAME`，1.0 按科目开头取字段（1开头 `TESTO_14`/`TESTO_15`，2开头 `TESTO_5`/`TESTO_6`），两侧客商编码 `LTRIM(·,'0')` 对齐，只输出有差异的行并标记"仅2.0有/仅1.0有"
- 已将知识库位置、结构索引、命名规范和关键业务口径沉淀到 `MEMORY.md`，后续字段命名与口径直接引用该文件

## 补充 AW_MR9_ARPM01/ARPM02 数据字典
- 数据源：`草稿/经分二期-应收-数据模型详细设计文档.xlsx`（41个sheet）
  - sheet15 `ARPM01账龄计算底稿-STEP1数据聚合` → 73字段
  - sheet18 `ARPM02-账龄结果表` → 110字段
  - sheet16/17（STEP2数据范围限制 / STEP3重分类处理）补充了 STEP1 留空字段的规则
- 已写入知识库：ARPM01@1646行、ARPM02@1746行；目录计数 21→23张、模块 185→187张
- 生成脚本：`草稿/_gen_arpm_dict.py`（解析xlsx→md）+ `草稿/_insert_kb.py`（插入知识库，含重复插入保护），设计文档更新后可重跑
- 两张表均为 DWM 层：ARPM01 存储过程 `CPM_SP_D2M_ARP_M`，ARPM02 存储过程 `CPM_SP_M2M_ARP_AG_M`
- 排查校验 SQL 报 ORA-00936：磁盘上两个校验文件（科目级/客商级）经全角字符扫描、括号配平、空IN()检查及 sqlglot Oracle 方言解析均通过，判定为 PL/SQL Developer 中执行的文本与磁盘版本不一致（常见：手工加"是否调整"列时漏逗号、或 azi 的 IN () 留空）
